Excercise no 7
Use reduce to compute the sum of the squares of all of the numbers in an array:

let array = [3, 5, 7];

function sumOfSquares(arrInput) {
  return arrInput.reduce((acc, elmnt) => (acc + (elmnt * elmnt)), 0); 
}

console.log(sumOfSquares(array)); // => 83